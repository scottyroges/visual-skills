/** Shared constants for the review-shell renderers. */

/** Risk-level → short uppercase label, used by the TL;DR card chip and the topbar risk chip. */
export const RISK_LABEL = { low: "LOW", med: "MED", high: "HIGH" } as const;
